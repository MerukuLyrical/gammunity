<div class="contents row">
  <div class="container">
    <%= form_with(model: @tpic, local: true) do |form| %>
      <h3>編集する</h3>
      <%= render partial: "form", locals: { tpic: @tpic } %>
    <% end %>
  </div>
</div>